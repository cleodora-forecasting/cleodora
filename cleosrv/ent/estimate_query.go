// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/cleodora-forecasting/cleodora/cleosrv/ent/estimate"
	"github.com/cleodora-forecasting/cleodora/cleosrv/ent/forecast"
	"github.com/cleodora-forecasting/cleodora/cleosrv/ent/predicate"
	"github.com/cleodora-forecasting/cleodora/cleosrv/ent/probability"
)

// EstimateQuery is the builder for querying Estimate entities.
type EstimateQuery struct {
	config
	ctx                    *QueryContext
	order                  []OrderFunc
	inters                 []Interceptor
	predicates             []predicate.Estimate
	withForecast           *ForecastQuery
	withProbabilities      *ProbabilityQuery
	withFKs                bool
	modifiers              []func(*sql.Selector)
	loadTotal              []func(context.Context, []*Estimate) error
	withNamedProbabilities map[string]*ProbabilityQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EstimateQuery builder.
func (eq *EstimateQuery) Where(ps ...predicate.Estimate) *EstimateQuery {
	eq.predicates = append(eq.predicates, ps...)
	return eq
}

// Limit the number of records to be returned by this query.
func (eq *EstimateQuery) Limit(limit int) *EstimateQuery {
	eq.ctx.Limit = &limit
	return eq
}

// Offset to start from.
func (eq *EstimateQuery) Offset(offset int) *EstimateQuery {
	eq.ctx.Offset = &offset
	return eq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eq *EstimateQuery) Unique(unique bool) *EstimateQuery {
	eq.ctx.Unique = &unique
	return eq
}

// Order specifies how the records should be ordered.
func (eq *EstimateQuery) Order(o ...OrderFunc) *EstimateQuery {
	eq.order = append(eq.order, o...)
	return eq
}

// QueryForecast chains the current query on the "forecast" edge.
func (eq *EstimateQuery) QueryForecast() *ForecastQuery {
	query := (&ForecastClient{config: eq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(estimate.Table, estimate.FieldID, selector),
			sqlgraph.To(forecast.Table, forecast.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, estimate.ForecastTable, estimate.ForecastColumn),
		)
		fromU = sqlgraph.SetNeighbors(eq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProbabilities chains the current query on the "probabilities" edge.
func (eq *EstimateQuery) QueryProbabilities() *ProbabilityQuery {
	query := (&ProbabilityClient{config: eq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(estimate.Table, estimate.FieldID, selector),
			sqlgraph.To(probability.Table, probability.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, estimate.ProbabilitiesTable, estimate.ProbabilitiesColumn),
		)
		fromU = sqlgraph.SetNeighbors(eq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Estimate entity from the query.
// Returns a *NotFoundError when no Estimate was found.
func (eq *EstimateQuery) First(ctx context.Context) (*Estimate, error) {
	nodes, err := eq.Limit(1).All(setContextOp(ctx, eq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{estimate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eq *EstimateQuery) FirstX(ctx context.Context) *Estimate {
	node, err := eq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Estimate ID from the query.
// Returns a *NotFoundError when no Estimate ID was found.
func (eq *EstimateQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eq.Limit(1).IDs(setContextOp(ctx, eq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{estimate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eq *EstimateQuery) FirstIDX(ctx context.Context) int {
	id, err := eq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Estimate entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Estimate entity is found.
// Returns a *NotFoundError when no Estimate entities are found.
func (eq *EstimateQuery) Only(ctx context.Context) (*Estimate, error) {
	nodes, err := eq.Limit(2).All(setContextOp(ctx, eq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{estimate.Label}
	default:
		return nil, &NotSingularError{estimate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eq *EstimateQuery) OnlyX(ctx context.Context) *Estimate {
	node, err := eq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Estimate ID in the query.
// Returns a *NotSingularError when more than one Estimate ID is found.
// Returns a *NotFoundError when no entities are found.
func (eq *EstimateQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eq.Limit(2).IDs(setContextOp(ctx, eq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{estimate.Label}
	default:
		err = &NotSingularError{estimate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eq *EstimateQuery) OnlyIDX(ctx context.Context) int {
	id, err := eq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Estimates.
func (eq *EstimateQuery) All(ctx context.Context) ([]*Estimate, error) {
	ctx = setContextOp(ctx, eq.ctx, "All")
	if err := eq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Estimate, *EstimateQuery]()
	return withInterceptors[[]*Estimate](ctx, eq, qr, eq.inters)
}

// AllX is like All, but panics if an error occurs.
func (eq *EstimateQuery) AllX(ctx context.Context) []*Estimate {
	nodes, err := eq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Estimate IDs.
func (eq *EstimateQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	ctx = setContextOp(ctx, eq.ctx, "IDs")
	if err := eq.Select(estimate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eq *EstimateQuery) IDsX(ctx context.Context) []int {
	ids, err := eq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eq *EstimateQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, eq.ctx, "Count")
	if err := eq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, eq, querierCount[*EstimateQuery](), eq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (eq *EstimateQuery) CountX(ctx context.Context) int {
	count, err := eq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eq *EstimateQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, eq.ctx, "Exist")
	switch _, err := eq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (eq *EstimateQuery) ExistX(ctx context.Context) bool {
	exist, err := eq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EstimateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eq *EstimateQuery) Clone() *EstimateQuery {
	if eq == nil {
		return nil
	}
	return &EstimateQuery{
		config:            eq.config,
		ctx:               eq.ctx.Clone(),
		order:             append([]OrderFunc{}, eq.order...),
		inters:            append([]Interceptor{}, eq.inters...),
		predicates:        append([]predicate.Estimate{}, eq.predicates...),
		withForecast:      eq.withForecast.Clone(),
		withProbabilities: eq.withProbabilities.Clone(),
		// clone intermediate query.
		sql:  eq.sql.Clone(),
		path: eq.path,
	}
}

// WithForecast tells the query-builder to eager-load the nodes that are connected to
// the "forecast" edge. The optional arguments are used to configure the query builder of the edge.
func (eq *EstimateQuery) WithForecast(opts ...func(*ForecastQuery)) *EstimateQuery {
	query := (&ForecastClient{config: eq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	eq.withForecast = query
	return eq
}

// WithProbabilities tells the query-builder to eager-load the nodes that are connected to
// the "probabilities" edge. The optional arguments are used to configure the query builder of the edge.
func (eq *EstimateQuery) WithProbabilities(opts ...func(*ProbabilityQuery)) *EstimateQuery {
	query := (&ProbabilityClient{config: eq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	eq.withProbabilities = query
	return eq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Reason string `json:"reason,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Estimate.Query().
//		GroupBy(estimate.FieldReason).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (eq *EstimateQuery) GroupBy(field string, fields ...string) *EstimateGroupBy {
	eq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EstimateGroupBy{build: eq}
	grbuild.flds = &eq.ctx.Fields
	grbuild.label = estimate.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Reason string `json:"reason,omitempty"`
//	}
//
//	client.Estimate.Query().
//		Select(estimate.FieldReason).
//		Scan(ctx, &v)
func (eq *EstimateQuery) Select(fields ...string) *EstimateSelect {
	eq.ctx.Fields = append(eq.ctx.Fields, fields...)
	sbuild := &EstimateSelect{EstimateQuery: eq}
	sbuild.label = estimate.Label
	sbuild.flds, sbuild.scan = &eq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EstimateSelect configured with the given aggregations.
func (eq *EstimateQuery) Aggregate(fns ...AggregateFunc) *EstimateSelect {
	return eq.Select().Aggregate(fns...)
}

func (eq *EstimateQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range eq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, eq); err != nil {
				return err
			}
		}
	}
	for _, f := range eq.ctx.Fields {
		if !estimate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eq.path != nil {
		prev, err := eq.path(ctx)
		if err != nil {
			return err
		}
		eq.sql = prev
	}
	return nil
}

func (eq *EstimateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Estimate, error) {
	var (
		nodes       = []*Estimate{}
		withFKs     = eq.withFKs
		_spec       = eq.querySpec()
		loadedTypes = [2]bool{
			eq.withForecast != nil,
			eq.withProbabilities != nil,
		}
	)
	if eq.withForecast != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, estimate.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Estimate).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Estimate{config: eq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(eq.modifiers) > 0 {
		_spec.Modifiers = eq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := eq.withForecast; query != nil {
		if err := eq.loadForecast(ctx, query, nodes, nil,
			func(n *Estimate, e *Forecast) { n.Edges.Forecast = e }); err != nil {
			return nil, err
		}
	}
	if query := eq.withProbabilities; query != nil {
		if err := eq.loadProbabilities(ctx, query, nodes,
			func(n *Estimate) { n.Edges.Probabilities = []*Probability{} },
			func(n *Estimate, e *Probability) { n.Edges.Probabilities = append(n.Edges.Probabilities, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range eq.withNamedProbabilities {
		if err := eq.loadProbabilities(ctx, query, nodes,
			func(n *Estimate) { n.appendNamedProbabilities(name) },
			func(n *Estimate, e *Probability) { n.appendNamedProbabilities(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range eq.loadTotal {
		if err := eq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (eq *EstimateQuery) loadForecast(ctx context.Context, query *ForecastQuery, nodes []*Estimate, init func(*Estimate), assign func(*Estimate, *Forecast)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Estimate)
	for i := range nodes {
		if nodes[i].forecast_estimates == nil {
			continue
		}
		fk := *nodes[i].forecast_estimates
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(forecast.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "forecast_estimates" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (eq *EstimateQuery) loadProbabilities(ctx context.Context, query *ProbabilityQuery, nodes []*Estimate, init func(*Estimate), assign func(*Estimate, *Probability)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Estimate)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Probability(func(s *sql.Selector) {
		s.Where(sql.InValues(estimate.ProbabilitiesColumn, fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.estimate_probabilities
		if fk == nil {
			return fmt.Errorf(`foreign-key "estimate_probabilities" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "estimate_probabilities" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (eq *EstimateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eq.querySpec()
	if len(eq.modifiers) > 0 {
		_spec.Modifiers = eq.modifiers
	}
	_spec.Node.Columns = eq.ctx.Fields
	if len(eq.ctx.Fields) > 0 {
		_spec.Unique = eq.ctx.Unique != nil && *eq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, eq.driver, _spec)
}

func (eq *EstimateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   estimate.Table,
			Columns: estimate.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: estimate.FieldID,
			},
		},
		From:   eq.sql,
		Unique: true,
	}
	if unique := eq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := eq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, estimate.FieldID)
		for i := range fields {
			if fields[i] != estimate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := eq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eq *EstimateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eq.driver.Dialect())
	t1 := builder.Table(estimate.Table)
	columns := eq.ctx.Fields
	if len(columns) == 0 {
		columns = estimate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eq.sql != nil {
		selector = eq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eq.ctx.Unique != nil && *eq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range eq.predicates {
		p(selector)
	}
	for _, p := range eq.order {
		p(selector)
	}
	if offset := eq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedProbabilities tells the query-builder to eager-load the nodes that are connected to the "probabilities"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (eq *EstimateQuery) WithNamedProbabilities(name string, opts ...func(*ProbabilityQuery)) *EstimateQuery {
	query := (&ProbabilityClient{config: eq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if eq.withNamedProbabilities == nil {
		eq.withNamedProbabilities = make(map[string]*ProbabilityQuery)
	}
	eq.withNamedProbabilities[name] = query
	return eq
}

// EstimateGroupBy is the group-by builder for Estimate entities.
type EstimateGroupBy struct {
	selector
	build *EstimateQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (egb *EstimateGroupBy) Aggregate(fns ...AggregateFunc) *EstimateGroupBy {
	egb.fns = append(egb.fns, fns...)
	return egb
}

// Scan applies the selector query and scans the result into the given value.
func (egb *EstimateGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, egb.build.ctx, "GroupBy")
	if err := egb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EstimateQuery, *EstimateGroupBy](ctx, egb.build, egb, egb.build.inters, v)
}

func (egb *EstimateGroupBy) sqlScan(ctx context.Context, root *EstimateQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(egb.fns))
	for _, fn := range egb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*egb.flds)+len(egb.fns))
		for _, f := range *egb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*egb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := egb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EstimateSelect is the builder for selecting fields of Estimate entities.
type EstimateSelect struct {
	*EstimateQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (es *EstimateSelect) Aggregate(fns ...AggregateFunc) *EstimateSelect {
	es.fns = append(es.fns, fns...)
	return es
}

// Scan applies the selector query and scans the result into the given value.
func (es *EstimateSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, es.ctx, "Select")
	if err := es.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EstimateQuery, *EstimateSelect](ctx, es.EstimateQuery, es, es.inters, v)
}

func (es *EstimateSelect) sqlScan(ctx context.Context, root *EstimateQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(es.fns))
	for _, fn := range es.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*es.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := es.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
