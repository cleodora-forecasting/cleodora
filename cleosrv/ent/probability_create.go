// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/cleodora-forecasting/cleodora/cleosrv/ent/estimate"
	"github.com/cleodora-forecasting/cleodora/cleosrv/ent/outcome"
	"github.com/cleodora-forecasting/cleodora/cleosrv/ent/probability"
)

// ProbabilityCreate is the builder for creating a Probability entity.
type ProbabilityCreate struct {
	config
	mutation *ProbabilityMutation
	hooks    []Hook
}

// SetValue sets the "value" field.
func (pc *ProbabilityCreate) SetValue(i int) *ProbabilityCreate {
	pc.mutation.SetValue(i)
	return pc
}

// SetEstimateID sets the "estimate" edge to the Estimate entity by ID.
func (pc *ProbabilityCreate) SetEstimateID(id int) *ProbabilityCreate {
	pc.mutation.SetEstimateID(id)
	return pc
}

// SetNillableEstimateID sets the "estimate" edge to the Estimate entity by ID if the given value is not nil.
func (pc *ProbabilityCreate) SetNillableEstimateID(id *int) *ProbabilityCreate {
	if id != nil {
		pc = pc.SetEstimateID(*id)
	}
	return pc
}

// SetEstimate sets the "estimate" edge to the Estimate entity.
func (pc *ProbabilityCreate) SetEstimate(e *Estimate) *ProbabilityCreate {
	return pc.SetEstimateID(e.ID)
}

// SetOutcomeID sets the "outcome" edge to the Outcome entity by ID.
func (pc *ProbabilityCreate) SetOutcomeID(id int) *ProbabilityCreate {
	pc.mutation.SetOutcomeID(id)
	return pc
}

// SetNillableOutcomeID sets the "outcome" edge to the Outcome entity by ID if the given value is not nil.
func (pc *ProbabilityCreate) SetNillableOutcomeID(id *int) *ProbabilityCreate {
	if id != nil {
		pc = pc.SetOutcomeID(*id)
	}
	return pc
}

// SetOutcome sets the "outcome" edge to the Outcome entity.
func (pc *ProbabilityCreate) SetOutcome(o *Outcome) *ProbabilityCreate {
	return pc.SetOutcomeID(o.ID)
}

// Mutation returns the ProbabilityMutation object of the builder.
func (pc *ProbabilityCreate) Mutation() *ProbabilityMutation {
	return pc.mutation
}

// Save creates the Probability in the database.
func (pc *ProbabilityCreate) Save(ctx context.Context) (*Probability, error) {
	return withHooks[*Probability, ProbabilityMutation](ctx, pc.sqlSave, pc.mutation, pc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (pc *ProbabilityCreate) SaveX(ctx context.Context) *Probability {
	v, err := pc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pc *ProbabilityCreate) Exec(ctx context.Context) error {
	_, err := pc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pc *ProbabilityCreate) ExecX(ctx context.Context) {
	if err := pc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pc *ProbabilityCreate) check() error {
	if _, ok := pc.mutation.Value(); !ok {
		return &ValidationError{Name: "value", err: errors.New(`ent: missing required field "Probability.value"`)}
	}
	if v, ok := pc.mutation.Value(); ok {
		if err := probability.ValueValidator(v); err != nil {
			return &ValidationError{Name: "value", err: fmt.Errorf(`ent: validator failed for field "Probability.value": %w`, err)}
		}
	}
	return nil
}

func (pc *ProbabilityCreate) sqlSave(ctx context.Context) (*Probability, error) {
	if err := pc.check(); err != nil {
		return nil, err
	}
	_node, _spec := pc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	pc.mutation.id = &_node.ID
	pc.mutation.done = true
	return _node, nil
}

func (pc *ProbabilityCreate) createSpec() (*Probability, *sqlgraph.CreateSpec) {
	var (
		_node = &Probability{config: pc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: probability.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: probability.FieldID,
			},
		}
	)
	if value, ok := pc.mutation.Value(); ok {
		_spec.SetField(probability.FieldValue, field.TypeInt, value)
		_node.Value = value
	}
	if nodes := pc.mutation.EstimateIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   probability.EstimateTable,
			Columns: []string{probability.EstimateColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: estimate.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.estimate_probabilities = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := pc.mutation.OutcomeIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   probability.OutcomeTable,
			Columns: []string{probability.OutcomeColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: outcome.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.outcome_probabilities = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// ProbabilityCreateBulk is the builder for creating many Probability entities in bulk.
type ProbabilityCreateBulk struct {
	config
	builders []*ProbabilityCreate
}

// Save creates the Probability entities in the database.
func (pcb *ProbabilityCreateBulk) Save(ctx context.Context) ([]*Probability, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pcb.builders))
	nodes := make([]*Probability, len(pcb.builders))
	mutators := make([]Mutator, len(pcb.builders))
	for i := range pcb.builders {
		func(i int, root context.Context) {
			builder := pcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ProbabilityMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pcb *ProbabilityCreateBulk) SaveX(ctx context.Context) []*Probability {
	v, err := pcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pcb *ProbabilityCreateBulk) Exec(ctx context.Context) error {
	_, err := pcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pcb *ProbabilityCreateBulk) ExecX(ctx context.Context) {
	if err := pcb.Exec(ctx); err != nil {
		panic(err)
	}
}
